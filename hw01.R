censor <- function(notdead)
{ notdead = is.numeric(lost + bags + explode)
  worm <- function(live)
  for i {live[i] = live[i-1] - notdead}
  worm
  }
censor

//livefinal=live - dead - censor
censor = lost + bags + explode 